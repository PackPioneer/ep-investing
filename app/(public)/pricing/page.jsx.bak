import { redirect } from "next/navigation";

// Pricing is hidden during the free-to-join phase. The full pricing page is
// preserved as page.jsx.bak — restore it when freemium launches.
export default function PricingPage() {
  redirect("/get-started");
}
